spring:
  application:
    name: menu-easy
  datasource:
    url: jdbc:mysql://db-project.cbygeykeiqsl.us-east-2.rds.amazonaws.com:3306/db_project
    username: root
    password: Easy.2024.project
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

aws:
  region: us-east-1
  s3:
    buckets:
      images: menueasy-pictures

main:
  web-application-type: servlet

servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB

codec:
  max-in-memory-size: 10MB


server:
  port: 8080
  error:
    include-stacktrace: never


project:
  image: /app/images
  secretKey: ${PROJECT_SECRET_KEY}
  frontendPath: ${PROJECT_FRONTEND_PATH}


logging:
  level:
    org:
      springframework:
        security: DEBUG


